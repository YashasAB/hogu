
{
  "name": "hogu",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "concurrently -n api,web -c blue,magenta \"npm --prefix apps/api run dev\" \"npm --prefix apps/web run dev\"",
    "build": "npm --prefix apps/web run build && npm --prefix apps/api run build",
    "build:api": "npm --prefix apps/api run build",
    "build:web": "npm --prefix apps/web run build",
    "start": "npm --prefix apps/api run start",
    "migrate": "cd apps/api && npx prisma migrate deploy"
  },
  "devDependencies": {
    "@types/mime-types": "^3.0.1",
    "concurrently": "^9.0.1"
  },
  "dependencies": {
    "@replit/object-storage": "^1.0.0",
    "@types/leaflet": "^1.9.20",
    "@types/multer": "^2.0.0",
    "leaflet": "^1.9.4",
    "multer": "^2.0.2",
    "react-day-picker": "^9.8.1",
    "react-leaflet": "^5.0.0"
  }
}
