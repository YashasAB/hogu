{
  "name": "hogu",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "concurrently -n api,web -c blue,magenta \"pnpm -C apps/api dev\" \"pnpm -C apps/web dev\"",
    "build": "pnpm -C apps/web build && pnpm -C apps/api build",
    "start": "pnpm -C apps/api start"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  },
  "dependencies": {
    "@types/leaflet": "^1.9.20",
    "leaflet": "^1.9.4",
    "react-day-picker": "^9.8.1",
    "react-leaflet": "^5.0.0"
  }
}
